# Sentry 最小接入（计划中）

> 阶段：**31–60 天**（见 `docs/DECISION_EXECUTION_SYSTEM.md`）。当前可跳过。

---

## 目标

- 捕获 Godot 运行时未处理异常
- 能定位到场景/脚本大致模块

---

## 接入前条件

- [ ] 已有稳定周节奏（`PROJECT_STATUS` 指标连续 2 周非空）
- [ ] `verify_project.py` 默认绿

---

## 最小步骤（待做时勾选）

1. [ ] 注册 Sentry 项目，拿到 DSN（勿提交到公开仓库）
2. [ ] 使用环境变量或本地 `user://` 配置存放 DSN
3. [ ] 仅上报：未捕获错误 + 版本号 + 平台
4. [ ] 试触发 1 次测试错误，确认面板可见
5. [ ] 在 `RISK_REGISTER` 记一条：第三方监控数据隐私

---

## 回退

移除初始化代码 → 确认无 DSN 时游戏正常启动 → 跑 `verify_project.py`。
