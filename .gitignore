.godot/
.import/
.mono/

# Exports (Godot default) — presets are safe to commit; credentials stay local
export.cfg
.godot/export_credentials.cfg

# Editor / OS
.vscode/
.idea/
*.swp
Thumbs.db
Desktop.ini

# Logs / caches
*.log

# Export artifacts (CI builds to build/web; local export same path)
build/

# Python helper script caches (repo includes validate_exp_log.py)
__pycache__/
*.pyc

# Local transient files
tmp/
*.tmp
*.bak
*.orig
.vendor/
tools/_*.txt

# IDE / agent tooling (not part of the game)
.cursor/

# Bundled reference repos (keep outside game source tree)
agency-agents-main/
superpowers-main/
andrej-karpathy-skills-main/

# Unrelated scratch folder
1/


.vercel
